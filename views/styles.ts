'use strict';

import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles({
	ctn: {
		flex: '1', margin: '16px', minWidth: '45%',
		// FIX-ME Support flexible card of notes.
	},
});
